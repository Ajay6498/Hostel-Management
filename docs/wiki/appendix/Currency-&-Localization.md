# Currency & Localization (INR)

- Currency formatting uses `Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR' })`
- Utility: `src/utils/currency.js` → `formatINR(amount)`
- Dashboard and Rooms show values in INR by default.

